<controls:FancyWindow xmlns="https://spacestation14.io"
    xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    SetSize="995 600"
    MinSize="600 450">

    <BoxContainer Orientation="Vertical" VerticalExpand="True">
        <TabContainer Name="TabContainer" VerticalExpand="True">
            
            <!-- ВКЛАДКА ЗАКАЗОВ (Главная) -->
            <BoxContainer Orientation="Horizontal" VerticalExpand="True" Margin="8 4 8 6">
                
                <!-- ЛЕВАЯ ЧАСТЬ: Инфо и Поиск товаров -->
                <BoxContainer Orientation="Vertical" SeparationOverride="4" Margin="0 0 8 0" HorizontalExpand="True">
                    
                    <!-- Верхняя панель инфо -->
                    <BoxContainer Orientation="Vertical">
                        <GridContainer Columns="3">
                            <!-- Account -->
                            <Label Text="{Loc 'cargo-console-menu-account-name-label'}" StyleClasses="LabelKeyText" />
                            <PanelContainer StyleClasses="LowDivider" Margin="0 -2"/>
                            <RichTextLabel Name="AccountNameLabel" Text="{Loc 'cargo-console-menu-account-name-none-text'}" Margin="4 0"/>

                            <!-- Balance -->
                            <Label Text="{Loc 'cargo-console-menu-points-label'}" StyleClasses="LabelKeyText"/>
                            <PanelContainer StyleClasses="LowDivider" Margin="0 -2"/>
                            <RichTextLabel Name="PointsLabel" Text="$0" Margin="4 0" />
                        </GridContainer>
                        <PanelContainer StyleClasses="LowDivider" Margin="0 4 -8 0"/>
                    </BoxContainer>

                    <!-- Поиск и категории -->
                    <BoxContainer Orientation="Horizontal" Margin="0 2 0 0">
                        <LineEdit Name="SearchBar" 
                                  PlaceHolder="{Loc 'cargo-console-menu-search-bar-placeholder'}" 
                                  HorizontalExpand="True" />
                        <OptionButton Name="Categories" 
                                      Prefix="{Loc 'cargo-console-menu-categories-label'}" 
                                      StyleClasses="OpenLeft"/>
                    </BoxContainer>

                    <!-- Список товаров -->
                    <ScrollContainer VerticalExpand="True" HScrollEnabled="False">
                        <BoxContainer Name="Products" Orientation="Vertical" HorizontalExpand="True" />
                    </ScrollContainer>
                </BoxContainer>

                <PanelContainer StyleClasses="LowDivider" Margin="0 -8"/>

                <!-- ПРАВАЯ ЧАСТЬ: Список запросов/заказов -->
                <BoxContainer Orientation="Vertical" SizeFlagsStretchRatio="0.8" HorizontalExpand="True" Name="Orders">
                    
                    <!-- Заголовок запросов -->
                    <controls:StripeBack>
                        <Label Text="{Loc 'cargo-console-menu-requests-label'}" HorizontalAlignment="Center" Margin="4"/>
                    </controls:StripeBack>

                    <PanelContainer VerticalExpand="True" Margin="0 -4 0 0">
                        <PanelContainer.PanelOverride>
                            <gfx:StyleBoxFlat BackgroundColor="#040404" />
                        </PanelContainer.PanelOverride>
                        
                        <ScrollContainer VerticalExpand="True">
                            <BoxContainer Name="Requests" 
                                          Orientation="Vertical" 
                                          StyleClasses="transparentItemList" 
                                          VerticalExpand="True" 
                                          SeparationOverride="8" 
                                          Margin="8" />
                        </ScrollContainer>
                    </PanelContainer>
                </BoxContainer>
            </BoxContainer>

            <!-- ВКЛАДКА ФИНАНСОВ -->
            <BoxContainer Orientation="Vertical" Margin="15">
                <BoxContainer Orientation="Horizontal">
                    <RichTextLabel Name="TransferLimitLabel" Margin="0 0 15 0"/>
                    <RichTextLabel Name="UnlimitedNotifier" Text="{Loc 'cargo-console-menu-account-action-transfer-limit-unlimited-notifier'}"/>
                </BoxContainer>
                <BoxContainer Orientation="Horizontal">
                    <RichTextLabel Text="{Loc 'cargo-console-menu-account-action-select'}" Margin="0 0 10 0"/>
                    <OptionButton Name="ActionOptions"/>
                </BoxContainer>
                <Control MinHeight="5"/>
                <BoxContainer Orientation="Horizontal">
                    <RichTextLabel Name="AmountText" Text="{ Loc 'cargo-console-menu-account-action-amount'}"/>
                    <SpinBox Name="TransferSpinBox" MinWidth="100" Value="10"/>
                </BoxContainer>
                <Control MinHeight="15"/>
                <BoxContainer HorizontalAlignment="Center">
                    <Button Name="AccountActionButton" Text="{ Loc 'cargo-console-menu-account-action-button'}" MinHeight="45" MinWidth="120"/>
                </BoxContainer>
                <Control VerticalExpand="True"/>
                <BoxContainer VerticalAlignment="Bottom" HorizontalAlignment="Center">
                    <Button Name="AccountLimitToggleButton" Text="{ Loc 'cargo-console-menu-toggle-account-lock-button'}" MinHeight="45" MinWidth="120"/>
                </BoxContainer>
            </BoxContainer>
        </TabContainer>

        <!-- ФУТЕР (Нижняя полоса) -->
        <BoxContainer Orientation="Vertical" VerticalAlignment="Bottom">
            <PanelContainer StyleClasses="LowDivider" />
            <BoxContainer Orientation="Horizontal" Margin="12 0 6 2" VerticalAlignment="Bottom">
                <Label Text="{Loc 'cargo-console-menu-flavor-left'}" StyleClasses="WindowFooterText" />
                <Label Text="{Loc 'cargo-console-menu-flavor-right'}" StyleClasses="WindowFooterText" HorizontalAlignment="Right" HorizontalExpand="True" Margin="0 0 4 0" />
                <TextureRect StyleClasses="NTLogoDark" Stretch="KeepAspectCentered" VerticalAlignment="Center" HorizontalAlignment="Right" SetSize="19 19"/>
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>