<BoxContainer xmlns="https://spacestation14.io"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:humanoid="clr-namespace:Content.Client.Humanoid"
         xmlns:cc="clr-namespace:Content.Client.Administration.UI.CustomControls"
         xmlns:ui="clr-namespace:Content.Client.Lobby.UI"
         xmlns:traits="clr-namespace:Content.Client.ADT.Traits.UI"
         HorizontalExpand="True">
        <!-- Left side -->
        <BoxContainer Orientation="Vertical" Margin="10 10 10 10" HorizontalExpand="True">
            <!-- Middle container -->
            <BoxContainer Orientation="Horizontal" SeparationOverride="10" HorizontalExpand="True">
                <!-- Name box-->
                <BoxContainer Orientation="Vertical">
                    <ui:HighlightedContainer>
                        <BoxContainer Orientation="Vertical">
                            <ui:HighlightedContainer>
                                <BoxContainer Orientation="Vertical">
                                    <BoxContainer Orientation="Horizontal" VerticalExpand="True">
                                        <Label Text="{Loc 'humanoid-profile-editor-name-label'}" />
                                        <LineEdit Name="NameEdit" Access="Public" MinSize="270 0" VerticalAlignment="Center" Margin="5 0 0 0" />
                                        <Button Name="NameRandomize" Access="Public" Text="{Loc 'humanoid-profile-editor-name-random-button'}" />
                                    </BoxContainer>
                                    <Button Name="RandomizeEverythingButton" Access="Public" HorizontalAlignment="Center"
                                            HorizontalExpand="False" MaxWidth="256"
                                            Text="{Loc 'humanoid-profile-editor-randomize-everything-button'}" />
                                    <RichTextLabel Name="WarningLabel" Access="Public" HorizontalExpand="False"
                                                   VerticalExpand="True" MaxWidth="425"
                                                   HorizontalAlignment="Left" />
                                </BoxContainer>
                            </ui:HighlightedContainer>
                        </BoxContainer>
                    </ui:HighlightedContainer>
                </BoxContainer>
                <!-- Import/Export -->
                <BoxContainer Orientation="Vertical" MinSize="60 0" HorizontalExpand="True" HorizontalAlignment="Right">
                    <ui:HighlightedContainer Name="ProfileHighlight">
                        <BoxContainer Orientation="Vertical">
                            <Button Name="SaveButton" Access="Public" Text="{Loc 'humanoid-profile-editor-save-button'}"/>
                            <Button Name="ResetButton" Access="Public" Disabled="True" Text="{Loc 'humanoid-profile-editor-reset-button'}"/>
                            <Button Name="ImportButton" Access="Public" Text="{Loc 'humanoid-profile-editor-import-button'}"/>
                            <Button Name="ExportButton" Access="Public" Text="{Loc 'humanoid-profile-editor-export-button'}"/>
                            <Button Name="ExportImageButton" Text="{Loc 'humanoid-profile-editor-export-image-button'}"/>
                            <Button Name="OpenImagesButton" Text="{Loc 'humanoid-profile-editor-open-image-button'}"/>
                        </BoxContainer>
                    </ui:HighlightedContainer>
                </BoxContainer>
            </BoxContainer>
            <Control MinHeight="10" />
            <!-- tabContainer -->
            <TabContainer Name="TabContainer" Access="Public" VerticalExpand="True">
                <!-- Appearance Tab -->
                <BoxContainer Orientation="Vertical">
                    <ScrollContainer VerticalExpand="True">
                        <!-- appearanceList -->
                        <BoxContainer Orientation="Vertical">
                            <BoxContainer Margin="10" Orientation="Vertical" HorizontalExpand="True">
                                <!-- Species -->
                                <BoxContainer HorizontalExpand="True">
                                    <Label Text="{Loc 'humanoid-profile-editor-species-label'}" />
                                    <Control HorizontalExpand="True"/>
                                    <TextureButton Name="SpeciesInfoButton" Scale="0.3 0.3"
                                                   VerticalAlignment="Center"
                                                   ToolTip="{Loc 'humanoid-profile-editor-guidebook-button-tooltip'}"
                                                   Visible="False"/>
                                    <OptionButton Name="SpeciesButton" HorizontalAlignment="Right" Visible="False"/>
                                    <Button Name="NewSpeciesButton" HorizontalAlignment="Right" ToggleMode="True" />
                                </BoxContainer>
                                <!-- Age -->
                                <BoxContainer HorizontalExpand="True">
                                    <Label Text="{Loc 'humanoid-profile-editor-age-label'}" />
                                    <Control HorizontalExpand="True"/>
                                    <LineEdit Name="AgeEdit" MinSize="60 0" HorizontalAlignment="Right" />
                                </BoxContainer>
                                <!-- Sex -->
                                <BoxContainer HorizontalExpand="True">
                                    <Label Text="{Loc 'humanoid-profile-editor-sex-label'}" />
                                    <Control HorizontalExpand="True"/>
                                    <OptionButton Name="SexButton" HorizontalAlignment="Right" />
                                </BoxContainer>
                                <!-- Pronouns -->
                                <BoxContainer HorizontalExpand="True">
                                    <Label Text="{Loc 'humanoid-profile-editor-pronouns-label'}" />
                                    <Control HorizontalExpand="True"/>
                                    <OptionButton Name="PronounsButton" HorizontalAlignment="Right" />
                                </BoxContainer>
                                <!-- Show clothing -->
                                <BoxContainer HorizontalExpand="True">
                                    <Label Text="{Loc 'humanoid-profile-editor-clothing'}" />
                                    <Control HorizontalExpand="True"/>
                                    <Button Name="ShowClothes" Access="Public" Pressed="True" ToggleMode="True" Text="{Loc 'humanoid-profile-editor-clothing-show'}" HorizontalAlignment="Right" />
                                </BoxContainer>
                                <!-- Spawn Priority -->
                                <BoxContainer HorizontalExpand="True">
                                    <Label Text="{Loc 'humanoid-profile-editor-spawn-priority-label'}" />
                                    <Control HorizontalExpand="True"/>
                                    <OptionButton Name="SpawnPriorityButton" HorizontalAlignment="Right" />
                                </BoxContainer>
                                <!-- ADT Barks Start -->
                                <BoxContainer HorizontalExpand="True" Visible="False" Name="BarksContainer">
                                    <Label Text="{Loc 'humanoid-profile-editor-bark-label'}" />
                                    <Control HorizontalExpand="True"/>
                                    <Button Name="BarkProtoButton" HorizontalAlignment="Right" MinWidth="150" />
                                    <Button Name="BarkPlayButton" Text="{Loc 'humanoid-profile-editor-voice-play'}" MaxWidth="80" />
                                </BoxContainer>
                                <!-- ADT Barks End -->
                            </BoxContainer>
                            <!-- Skin -->
                            <BoxContainer Margin="10" HorizontalExpand="True" Orientation="Vertical">
                                <Label Text="{Loc 'humanoid-profile-editor-skin-color-label'}" />
                                <Slider HorizontalExpand="True" Name="Skin" MinValue="0" MaxValue="100" Value="20" />
                                <BoxContainer Name="RgbSkinColorContainer" Visible="False" Orientation="Vertical" HorizontalExpand="True"></BoxContainer>
                            </BoxContainer>
                            <!-- Eyes -->
                            <BoxContainer Margin="10" Orientation="Vertical">
                                <Label Text="{Loc 'humanoid-profile-editor-eyes-label'}" />
                                <humanoid:EyeColorPicker Name="EyeColorPicker" />
                            </BoxContainer>
                            <!-- Hair -->
                            <BoxContainer Margin="10" Orientation="Horizontal">
                                <humanoid:SingleMarkingPicker MinHeight="500" Name="HairStylePicker" Category="Hair" />
                                <humanoid:SingleMarkingPicker MinHeight="500" Name="FacialHairPicker" Category="FacialHair" />
                            </BoxContainer>
                        </BoxContainer>
                    </ScrollContainer>
                </BoxContainer>

                <!-- ADT Languages Start -->
                <BoxContainer Orientation="Vertical" Margin="0 0 0 0">
                    <!-- Заголовок с прогресс-баром -->
                    <PanelContainer StyleClasses="LanguagesHeaderPanel" Margin="0 0 0 8">
                        <BoxContainer Orientation="Vertical" Margin="12 10">
                            <BoxContainer Orientation="Horizontal" Margin="0 0 0 8">
                                <Label Text="{Loc 'humanoid-profile-editor-languages-tab'}" 
                                       StyleClasses="LanguagesTitleLabel"/>
                                <Control HorizontalExpand="True"/>
                                <Label Name="LanguagesCountLabel" 
                                       StyleClasses="LanguagesStatLabel"
                                       Text="0 / 0"/>
                            </BoxContainer>
                            <!-- Прогресс-бар -->
                            <PanelContainer StyleClasses="LanguagesProgressBarBg" SetHeight="12">
                                <PanelContainer Name="LanguagesProgressBar"
                                                StyleClasses="LanguagesProgressBarFill"
                                                HorizontalExpand="True"
                                                HorizontalAlignment="Left"/>
                            </PanelContainer>
                        </BoxContainer>
                    </PanelContainer>

                    <!-- Кнопка сброса -->
                    <Button Name="SetDefaultLanguagesButton" 
                            Text="{Loc 'humanoid-profile-editor-default-langs'}"
                            Margin="10 0 10 8"/>

                    <PanelContainer StyleClasses="LanguagesSearchBar" Margin="10 0 10 8">
                        <LineEdit Name="LanguagesSearchBar"
                                PlaceHolder="{Loc 'language-editor-search-placeholder'}"
                                StyleClasses="LanguagesSearchInput"
                                Margin="10 8"/>
                    </PanelContainer>

                    <!-- Список языков -->
                    <ScrollContainer VerticalExpand="True" HScrollEnabled="False">
                        <BoxContainer Name="LanguagesList" Orientation="Vertical" Margin="10 0 10 0"/>
                    </ScrollContainer>
                </BoxContainer>
                <!-- ADT Languages End -->

                <!-- Jobs Tab -->
                <BoxContainer Orientation="Vertical">
                    <OptionButton Name="PreferenceUnavailableButton" />
                    <ScrollContainer VerticalExpand="True">
                        <BoxContainer Name="JobList" Orientation="Vertical" />
                    </ScrollContainer>
                </BoxContainer>

                <!-- Antags Tab -->
                <BoxContainer Orientation="Vertical" Margin="10">
                    <Label Text="{Loc 'humanoid-profile-editor-antags-tab'}" StyleClasses="LabelHeading" Margin="0 0 0 10" />
                    <ScrollContainer VerticalExpand="True" HScrollEnabled="False">
                        <BoxContainer Name="AntagGrid" Orientation="Vertical" SeparationOverride="8" />
                    </ScrollContainer>
                </BoxContainer>

                <!-- Traits Tab -->
                <BoxContainer Orientation="Vertical" Margin="10">
                    <traits:TraitsTab Name="Traits" />
                </BoxContainer>

                <!-- Markings Tab -->
                <BoxContainer Name="MarkingsTab" Orientation="Vertical" Margin="10">
                    <ScrollContainer VerticalExpand="True">
                        <humanoid:MarkingPicker Name="Markings" IgnoreCategories="Hair,FacialHair" />
                    </ScrollContainer>
                </BoxContainer>
            </TabContainer>
        </BoxContainer>
        <!-- Right side -->
        <BoxContainer Orientation="Vertical" VerticalExpand="True" VerticalAlignment="Center">
            <SpriteView Name="SpriteView" Scale="8 8" Margin="4" SizeFlagsStretchRatio="1" />
            <BoxContainer Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 5">
                <Button Name="SpriteRotateLeft" Text="◀" StyleClasses="OpenRight" />
                <cc:VSeparator Margin="2 0 3 0" />
                <Button Name="SpriteRotateRight" Text="▶" StyleClasses="OpenLeft" />
            </BoxContainer>
        </BoxContainer>
</BoxContainer>